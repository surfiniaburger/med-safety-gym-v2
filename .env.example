# SafeClaw Non-Secret Configuration
# Copy this file to .env and customize as needed.

# URL where the SafeClaw Hub (Governor) is running locally
SAFECLAW_HUB_URL="http://localhost:8000"
SAFECLAW_HUB_PORT=8000

# The LLM model routing handled by LiteLLM (e.g. gemini/gemini-2.5-flash or ollama/gemma3:1b)
LITELLM_MODEL="gemini/gemini-2.5-flash"

# NOTE: API keys and sensitive tokens (like TELEGRAM_BOT_TOKEN and GITHUB_TOKEN)
# are securely managed via the macOS Keychain and should NOT be placed here.
# Run `bash scripts/store_secrets.sh` to securely store your credentials.
